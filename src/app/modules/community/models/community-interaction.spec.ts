import { CommunityInteraction } from './community-interaction';

describe('CommunityInteraction', () => {
  it('should create an instance', () => {
    expect(new CommunityInteraction()).toBeTruthy();
  });
});
